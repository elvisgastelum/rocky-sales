# 2026-03-28 Cycle 019

## Goal

Add durable API-mocking foundations for both web apps and add validation that the consumer web root path works without route-level Playwright mocks.

## Plan

1. Run pre-chunk validation.
2. Use Context7 MSW docs patterns to scaffold browser + test setup in both web apps.
3. Add smoke tests proving MSW interception works.
4. Update consumer web e2e to avoid manual route mocking.
5. Run post-chunk validation and sync memory files.

## Actions

- Ran `npm run format && npx nx affected -t lint test typecheck --uncommitted --tui=false` before edits.
- Retrieved Context7 reference for MSW setup patterns (`setupWorker`, `setupServer`).
- Installed `msw` and initialized service worker assets in:
  - `apps/store-consumer-web/public/mockServiceWorker.js`
  - `apps/admin-web/public/mockServiceWorker.js`
- Added MSW handler/browser/server + test setup files for both web apps.
- Added smoke specs:
  - `apps/store-consumer-web/src/mocks/msw.smoke.spec.ts`
  - `apps/admin-web/src/mocks/msw.smoke.spec.ts`
- Updated both app `main.tsx` files to conditionally start MSW browser worker when `VITE_USE_MSW=true` in dev mode.
- Updated both Vite configs to load `src/test.setup.ts` for Vitest.
- Updated `apps/store-consumer-web-e2e/src/example.spec.ts` to remove Playwright route mocking.
- Ran post-validation and targeted consumer web e2e.

## Results

- Both web apps now have reusable MSW scaffolding for future endpoint mocks in tests and optional local dev mocking.
- MSW smoke tests pass in both web projects.
- Consumer web e2e now validates `/` without Playwright route stubs and still passes across all configured browsers.

## Learnings

- MSW setup is stable with a shared pattern per app (`handlers`, `browser`, `server`, `test.setup`) and keeps test APIs close to app boundaries.

## Next Move

Start `RCS-0001` and implement the first admin BFF home endpoint vertical slice.
