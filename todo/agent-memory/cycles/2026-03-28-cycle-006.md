# 2026-03-28 Cycle 006

## Goal

Make `CLAUDE.md` a link-style pointer to `AGENTS.md`.

## Plan

1. Replace duplicate Nx guidance in `CLAUDE.md` with a short redirect.
2. Keep `AGENTS.md` as single source of truth.
3. Update todo memory records.

## Actions

- Rewrote `CLAUDE.md` to a minimal pointer message directing to `AGENTS.md`.
- Updated todo tracking files to reflect this consolidation decision.

## Results

- Canonical instruction source is now unambiguous.
- Reduced maintenance risk of duplicated agent instructions.

## Learnings

- Redirect files are simpler than maintaining parallel instruction docs.

## Next Move

If desired, commit the agent setup + todo memory + `CLAUDE.md` redirect as one docs/chore commit.
