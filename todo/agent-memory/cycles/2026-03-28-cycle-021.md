# 2026-03-28 Cycle 021

## Goal

Start implementing the MSW test-only policy by auditing current code and removing any runtime/browser-worker usage.

## Plan

1. Run pre-chunk validation.
2. Audit current source for MSW runtime/browser-worker patterns.
3. Remove policy violations from app runtime and assets.
4. Run post-chunk validation and rescan.
5. Sync memory files.

## Actions

- Ran `npm run format && npx nx affected -t lint test typecheck --uncommitted --tui=false` before edits.
- Audited `apps/` for `setupWorker`, `VITE_USE_MSW`, and `mockServiceWorker` patterns.
- Removed runtime MSW startup from:
  - `apps/store-consumer-web/src/main.tsx`
  - `apps/admin-web/src/main.tsx`
- Removed browser-worker-only files/assets:
  - `apps/store-consumer-web/src/mocks/browser.ts`
  - `apps/admin-web/src/mocks/browser.ts`
  - `apps/store-consumer-web/public/mockServiceWorker.js`
  - `apps/admin-web/public/mockServiceWorker.js`
- Ran `npm run format && npx nx affected -t lint test typecheck --uncommitted --tui=false` after edits.
- Reran audit in `apps/` for runtime/browser-worker patterns.

## Results

- Current application source now conforms to MSW test-only policy.
- Web app tests remain green, including MSW smoke tests using `msw/node` test server setup.
- No `setupWorker`, `VITE_USE_MSW`, `mockServiceWorker`, or `msw/browser` usage remains in `apps/` source.

## Learnings

- For this monorepo, `msw/node` test harness setup is sufficient for unit/integration support and avoids runtime coupling.

## Next Move

Implement `RCS-0001` admin BFF home endpoint while preserving the MSW test-only policy boundaries.
