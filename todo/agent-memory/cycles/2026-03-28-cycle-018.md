# 2026-03-28 Cycle 018

## Goal

Fix consumer web root-path failure caused by `/api/consumer/home` returning `500` when the local consumer BFF is unavailable.

## Plan

1. Run pre-chunk validation.
2. Harden consumer web Vite proxy behavior for `/api` requests.
3. Run post-chunk validation and targeted checks.
4. Sync agent memory files.

## Actions

- Ran `npx nx affected -t lint test typecheck --uncommitted --tui=false` before edits.
- Updated `apps/store-consumer-web/vite.config.mts` to:
  - use `STORE_CONSUMER_BFF_URL` (default `http://localhost:3000`) as proxy target,
  - return fallback JSON for `/api/consumer/home` on proxy connection failures,
  - return structured `502` JSON for other `/api` proxy failures.
- Ran `npm run format && npx nx affected -t lint test typecheck --uncommitted --tui=false`.
- Ran `npx nx run @rocky-sales/store-consumer-bff-e2e:e2e -- --runTestsByPath src/store-consumer-bff/store-consumer-bff.spec.ts`.
- Ran `npx nx run @rocky-sales/store-consumer-web:serve` and `curl http://localhost:5500/api/consumer/home` with consumer BFF offline.

## Results

- `http://localhost:5500/api/consumer/home` now returns `200` with baseline payload when local BFF is unavailable.
- Consumer BFF contract remained valid via targeted BFF e2e pass.
- Affected lint/test/typecheck remained green after the change.

## Learnings

- Local proxy unavailability in Vite can surface as `500`; handling proxy `error` events provides a controlled developer experience without changing BFF contracts.

## Next Move

Add consumer web e2e coverage for both real BFF wiring and offline fallback behavior for `/api/consumer/home`.
