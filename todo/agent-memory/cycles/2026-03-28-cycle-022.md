# 2026-03-28 Cycle 022

## Goal

Add smoke unit tests that prove MSW interception works at unit-test runtime for each runtime app family.

## Plan

1. Keep existing web MSW smoke tests unchanged.
2. Add Jest + `msw/node` smoke specs for both BFF apps.
3. Run targeted tests and full affected verification.
4. Fix any runtime/tooling issues.

## Actions

- Added `apps/store-consumer-bff/src/mocks/msw.smoke.spec.ts`.
- Added `apps/admin-bff/src/mocks/msw.smoke.spec.ts`.
- Ran targeted tests for both new specs; both initially failed due to Jest ESM parsing for MSW dependency chain.
- Updated Jest configs in both BFF apps to transpile ESM dependencies used by MSW:
  - `apps/store-consumer-bff/jest.config.cts`
  - `apps/admin-bff/jest.config.cts`
- Re-ran targeted tests successfully.
- Ran required pre/post validation cadence:
  - `npm run format && npx nx affected -t lint test typecheck --uncommitted --tui=false`

## Results

- MSW runtime smoke coverage now exists for all runtime app families:
  - Web (Vitest): already present and unchanged.
  - BFF (Jest): newly added and passing.
- Affected lint/test/typecheck checks passed after config fix.

## Learnings

- MSW v2 dependency chain includes ESM modules that require Jest transform allowlisting in these BFF setups.
- Isolated smoke specs with `onUnhandledRequest: 'error'` keep behavior deterministic and policy-compliant.

## Next Move

Resume execution of `RCS-0001` to continue first admin vertical-slice delivery.
