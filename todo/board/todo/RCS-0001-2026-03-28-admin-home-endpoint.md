# RCS-0001 admin-home-endpoint

## Code

- `RCS-0001`

## Goal

Implement the admin BFF home endpoint `GET /api/admin/home` returning `{ title, message }`.

## Acceptance

- [ ] Endpoint exists in `admin-bff` controller/service.
- [ ] Unit test covers success response shape.
- [ ] Admin BFF e2e test validates endpoint response.

## Notes

Keep this card scoped to backend endpoint + tests only.
