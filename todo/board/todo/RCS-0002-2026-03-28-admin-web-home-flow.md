# RCS-0002 admin-web-home-flow

## Code

- `RCS-0002`

## Goal

Implement `admin-web` root flow to fetch `GET /api/admin/home` and render loading, success, and error states.

## Acceptance

- [ ] Root route fetches admin home payload using a typed local contract.
- [ ] UI renders explicit loading, success, and error states.
- [ ] Vitest coverage validates state transitions.

## Notes

Keep this card scoped to admin web integration only.
