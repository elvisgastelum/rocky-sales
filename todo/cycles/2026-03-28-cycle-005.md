# 2026-03-28 Cycle 005

## Goal

Adjust `rocky-sales-planner` permissions to allow safe context gathering while keeping planning-only behavior.

## Plan

1. Change planner bash policy to guarded mode.
2. Keep file edits blocked.
3. Sync repository docs and todo memory with the new policy.

## Actions

- Updated `.opencode/agents/rocky-sales-planner.md`:
  - Kept `edit: deny`.
  - Set `bash` to `'*': ask`.
  - Allowlisted `git status*`, `git log*`, `git diff*`, and `npx nx show*`.
- Updated `AGENTS.md` permission intent.
- Updated todo state, backlog, decisions, run-log, next-steps, and handoff context.

## Results

- Planner can gather better repo context without default execution freedom.
- Non-allowlisted commands still require explicit approval.
- Builder remains the implementation path.

## Learnings

- Strict `bash: deny` can hinder planning quality in large repos.
- Guarded allowlists provide better planning ergonomics with safety.

## Next Move

Restart OpenCode and validate planner behavior with one allowlisted command and one non-allowlisted command.
