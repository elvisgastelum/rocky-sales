# 2026-03-28 Cycle 003

## Goal

Make planner and builder available as OpenCode primary agents in Tab options.

## Plan

1. Create `.opencode/agents/` primary agent definitions.
2. Encode mandatory startup anchor in both agent prompts.
3. Update repository docs and todo memory for continuity.

## Actions

- Created `.opencode/agents/rocky-sales-planner.md` with `mode: primary` and planning constraints.
- Created `.opencode/agents/rocky-sales-builder.md` with `mode: primary` and implementation constraints.
- Updated `AGENTS.md` to point to `.opencode/agents/` primary profiles.
- Updated todo memory files for state, decisions, backlog, run-log, and handoff.

## Results

- `rocky-sales-planner` and `rocky-sales-builder` are now project-local primary agents.
- Agents remain git-tracked for cross-machine consistency.
- Mandatory todo startup anchor is enforced in both primary agent definitions.

## Learnings

- In OpenCode, Tab options come from primary agents in `.opencode/agents/`, not skills only.
- Keeping both `.agents/skills/` and `.opencode/agents/` provides both tool-driven and UI-driven routing.

## Next Move

Restart OpenCode to load new primary agents, then continue with lint baseline fix.
